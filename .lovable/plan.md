

## Hero & Navbar Updates

### Changes

1. **Remove "OPPRIME.tv" text logo from Navbar** — Replace with the uploaded logo image (waiting for logo file). Place logo at top-left in navbar as it currently is.

2. **Add large logo to hero section, top-center** — Place the logo image centered at the top of the hero section, sized similarly to "The Future of Independent Cinema" text line. This will sit above the headline.

3. **Remove "The Future of Independent Cinema" red text** — Replace with the large centered logo.

4. **Increase hero background image visibility** — Change `opacity-30` to `opacity-50` on the hero background image (roughly 25% more visible). Also lighten the gradient overlay from `from-background/70 via-background/85` to `from-background/60 via-background/75`.

### Waiting on
- **Logo file**: You mentioned you'd give me a logo — please upload it and I'll integrate it into both the navbar (top-left, small) and the hero (top-center, large).

